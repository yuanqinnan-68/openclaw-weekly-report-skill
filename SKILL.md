---
name: openclaw-weekly-report
description: 将飞书周报任务整理为简洁、完整、按状态排序的 HTML 邮件。适用于优化 OpenClaw 周报摘要、亮点提炼、项目明细、邮件模板和发送链路；不用于普通邮件写作。
---

# OpenClaw 周报总结

目标是把飞书原文整理成可快速阅读的周报，而不是逐字复制。默认处理链路为：读取飞书原文、逐条建模、按项目组织、大模型提炼、结构校验、状态排序、渲染 HTML、发送邮件。

## 不可破坏的规则

- 每条飞书记录都要展示；不要去重、合并或因为内容相似而丢弃。
- 同项目、同任务、同负责人在本周内容和下周规划中仍分别展示。
- 未填写的任务简介、本周进展或下周计划保持空白，不生成“暂无”，也不臆造内容。
- 大模型只改写任务名称、本周进展和下周计划；项目、负责人、状态等事实字段不得改写或补充。
- 项目明细要简短但完整：本周进展建议 20–50 字，下周计划建议 15–40 字，最多一句完整话。
- 不使用省略号，不硬切字符，不输出半句话。模型输出缺失、过长、包含步骤清单或结构非法时，对该条使用原文兜底。
- 本周亮点只从“已完成”的本周任务中选择最突出的 5 条，逐条介绍项目、关键动作和结果；不要把 5 条压缩成一段总计 100 字的文字。
- 每个项目表内按以下顺序稳定排序，同状态保持飞书原始顺序：已完成、卡住、调试中、对接中、设计中、推进中、待开始。
- 发送周报只能走一个端到端工具，不要再组合调用独立同步、分析和发邮件工具，以免重复发送。

## 工作方式

修改摘要或模板前，先阅读 [references/report-contract.md](references/report-contract.md)。涉及飞书同步、模型配置、部署或邮件发送时，再阅读 [references/integration.md](references/integration.md)。

优先对 `implementation/` 中的脱敏快照做小范围修改，并同步更新对应约束。任何线上部署都应先备份，且不得把生产密钥写入代码或提交到 GitHub。

验证时以数据不丢失为第一优先级：检查输入与项目详情行数一致，再检查状态排序、亮点选择、空字段、句子完整性和 HTML 邮件兼容性。除非用户明确授权，不发送真实测试邮件。
